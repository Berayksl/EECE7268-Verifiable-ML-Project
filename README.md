# STL Robustness Verification of NN Controllers

## Installing the requirements
```bash
pip install -r requirements.txt
```

## How to run
Run `python main.py --mode train` to start training from scratch.

To test a trained model, run: `python main.py --mode test` and select the trained actor network .pth file.




